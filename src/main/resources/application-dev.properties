# dev profile：启用模拟接口，跳过钉钉 Stream 连接
# 启动方式：java -Dspring.profiles.active=dev -jar app.jar
# 或 IDE 里设置 VM options: -Dspring.profiles.active=dev

server.port=8080
spring.thymeleaf.cache=false

# dev 模式下系统 API 可以不配置（模拟接口不会真正调用外部系统）
# 如需测试真实 API，仍需通过 -Dsys.username / -Dsys.password 传入
